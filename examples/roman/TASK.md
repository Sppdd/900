Implement `to_roman` and `from_roman` in roman.py so that all tests in
test_roman.py pass. `from_roman` must reject non-canonical numerals
(e.g. "IIII", "IC") with ValueError, and `to_roman` must reject values
outside 1..3999.
